import { createTheme } from '@mui/material/styles'

// Create a theme instance.
export const theme = createTheme({
  cssVariables: true,
  palette: {
    primary: {
      main: '#3FAE49'
    },
    secondary: {
      main: '#002953'
    },
    error: {
      main: '#bd6c82'
    },
    white: {
      main: '#ffffff'
    },
    third: {
      main: '#312783'
    },
    
  }
})
